# git init

# git --global (or not) config user.name ""
# git --global (or not) config user.email ""
# git config --list
# git remote add origin (ssh/https)
# git clone (ssh/https)
# git remote -v

# git status
# git add . (or file name)
# git restore --staged (file name)
# git commit -m "(message here)"
# git push

# git log
# git log --oneline
# git log --oneline --all
# git log --oneline --all --graph
# git switch --detach (#hash)
# git switch master (or main)
# git revert (#hash)

# git branch (name)
# git branch
# git switch (name)
# git branch -D (name)
